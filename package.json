{
  "name": "shawarmys",
  "private": true,
  "scripts": {
    "start": "concurrently -n db,server,client -c blue,green,cyan \"docker compose -f docker-compose.dev.yml up\" \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "start:db": "docker compose -f docker-compose.dev.yml up -d",
    "stop:db": "docker compose -f docker-compose.dev.yml down",
    "install:all": "cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
